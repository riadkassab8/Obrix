kind = "web"
previewPath = "/"
title = "Obrix — Landing Page"
version = "1.0.0"
id = "artifacts/obrix"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18279

[services.development]
run = "pnpm --filter @workspace/obrix run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/obrix", "run", "build" ]
publicDir = "artifacts/obrix/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18279"
BASE_PATH = "/"
